const menu=window.MENU||[]; const bar=document.querySelector('#catbar'), list=document.querySelector('#menuList');
if(bar&&list){const cats=[...new Set(menu.map(x=>x.category))]; const labels={'SOUPS/SALADS':'Soups + Salads','TAKE OUT SOUPS & SAUCES':'Take-Home Soups + Sauces'}; cats.forEach((c,i)=>{const b=document.createElement('button');b.textContent=labels[c]||c;b.dataset.cat=c;if(!i)b.classList.add('active');b.onclick=()=>document.querySelector('#cat-'+CSS.escape(c)).scrollIntoView({behavior:'smooth',block:'start'});bar.append(b); const g=document.createElement('section');g.className='menu-group';g.id='cat-'+c;g.innerHTML=`<h3>${labels[c]||c}</h3><div class="menu-items">${menu.filter(x=>x.category===c).map(x=>`<article class="menu-item"><h4><span>${x.name}</span><span>${x.price}</span></h4>${x.description?`<p>${x.description}</p>`:''}</article>`).join('')}</div>`;list.append(g)}); const groups=[...document.querySelectorAll('.menu-group')];const mio=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)document.querySelectorAll('.catbar button').forEach(b=>b.classList.toggle('active',b.dataset.cat===e.target.id.slice(4)))}),{rootMargin:'-20% 0px -70%'});groups.forEach(x=>mio.observe(x));}
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('on')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(x=>io.observe(x));const nb=document.querySelector('.navbtn');if(nb)nb.onclick=e=>{const n=document.querySelector('#nav');const o=n.classList.toggle('open');e.currentTarget.setAttribute('aria-expanded',o)};document.querySelectorAll('#nav a').forEach(a=>a.onclick=()=>document.querySelector('#nav').classList.remove('open'));